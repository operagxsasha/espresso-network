(function() {
    const implementors = Object.fromEntries([["vid",[["impl&lt;E, I&gt; DigestAlgorithm&lt;E, I, <a class=\"struct\" href=\"vid/utils/blake3/struct.Blake3Node.html\" title=\"struct vid::utils::blake3::Blake3Node\">Blake3Node</a>&gt; for <a class=\"struct\" href=\"vid/utils/blake3/struct.Blake3DigestAlgorithm.html\" title=\"struct vid::utils::blake3::Blake3DigestAlgorithm\">Blake3DigestAlgorithm</a><div class=\"where\">where\n    E: Element + CanonicalSerialize,\n    I: Index + CanonicalSerialize,</div>",0]]]]);
    if (window.register_implementors) {
        window.register_implementors(implementors);
    } else {
        window.pending_implementors = implementors;
    }
})()
//{"start":59,"fragment_lengths":[468]}